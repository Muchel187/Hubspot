exports.main = async (context = {}) => {
  // Simple OAuth callback handler
  // This is a placeholder function for HubSpot project structure

  return {
    statusCode: 200,
    body: {
      message: 'OAuth callback handler'
    }
  };
};